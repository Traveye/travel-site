<div class="mx-auto">
  <img src="{{logo.imagePath}}" class="logo" alt="{{logo.imageAlt}}" />
</div>

<div>
  <p>"Welcome to the ultimate travel companion for the adventurous and organized
    traveler! Our site is designed for people who love to travel and want an
    easy way to document all of their trips. Whether you're a seasoned traveler
    or just starting out, our site is the perfect way to help you organize and
    remember your travels. With our site, you can drop a pin anywhere on a map
    to create a visual representation of your travels. Simply click on the pin
    to input all your memories, like a journal. Our site offers separate places
    to store your favorite foods and other experiences, but the best part is
    that it's completely customizable to fit your unique needs. Say goodbye to
    forgotten adventures and hello to a beautifully organized record of your
    travels. Start documenting your trips today!"</p>
</div>



<!-- The Modal -->
<div>
  <div>
    <div></div>
  </div>

  <div
    class="bg-white rounded-lg px-4 pt-5 pb-4 overflow-hidden shadow-xl transform transition-all sm:max-w-lg sm:w-full"
  >
    <!-- The Modal Content -->
    <div class="">
      <!-- The Close Button -->
      <div class="flex justify-end">
        <button
          class="text-3xl leading-none hover:text-gray-500"
          @click="showModal = false"
        >
          &times;
        </button>
      </div>
      <!-- The Modal Title -->
      <h2 class="text-lg font-medium text-gray-900 pb-4">SignUp</h2>
      <!-- The Modal Body -->
      <div class="field">
        <label class="label">Username</label>
        <div class="control">
          <input class="input" type="text">
        </div>
      </div>
      <div class="field">
        <label class="label">Display Name</label>
        <div class="control">
          <input class="input" type="text">
        </div>
      </div>
      <div class="field">
        <label class="label">Password</label>
        <div class="control">
          <input class="input" type="password">
        </div>
      </div>
      <!-- The Modal Footer -->
      <div class="flex justify-end pt-2">
        <button
          class="text-sm font-medium bg-gray-100 hover:bg-gray-200 px-4 py-2 rounded-lg"
        >
          Cancel
        </button>
        <button
          class="text-sm font-medium bg-indigo-500 hover:bg-indigo-600 text-white px-4 py-2 rounded-lg ml-4"
        >
          Submit
        </button>
      </div>
    </div>
  </div>
</div>

<!-- The Modal -->
<div class="fixed bottom-0 inset-x-0 px-4 pb-4 sm:inset-0 sm:flex sm:items-center sm:justify-center">
  <div class="fixed inset-0 transition-opacity">
    <div class="absolute inset-0 bg-gray-500 opacity-75"></div>
  </div>

  <div class="bg-white rounded-lg px-4 pt-5 pb-4 overflow-hidden shadow-xl transform transition-all sm:max-w-lg sm:w-full">
    <!-- The Modal Content -->
    <div class="">
      <!-- The Close Button -->
      <div class="flex justify-end">
        <button class="text-3xl leading-none hover:text-gray-500" @click="showModal = false">
          &times;
        </button>
      </div>
      <!-- The Modal Title -->
      <h2 class="text-lg font-medium text-gray-900 pb-4">LogIn</h2>
      <!-- The Modal Body -->
            <div class="field">
        <label class="label">Username</label>
        <div class="control">
          <input class="input" type="text">
        </div>
      </div>
      <div class="field">
        <label class="label">Password</label>
        <div class="control">
          <input class="input" type="password">
        </div>
      </div>
      <!-- The Modal Footer -->
      <div class="flex justify-end pt-2">
        <button class="text-sm font-medium bg-gray-100 hover:bg-gray-200 px-4 py-2 rounded-lg">
          Cancel
        </button>
        <button class="text-sm font-medium bg-indigo-500 hover:bg-indigo-600 text-white px-4 py-2 rounded-lg ml-4">
          Submit
        </button>
      </div>
    </div>
  </div>
</div>


{{!-- overlay --}}
<div class="absolute inset-0 bg-black bg-opacity-30 h-screen w-full flex justify-center items-start md:items-center pt-10 md:pt-0">

  {{!-- modal --}}
  <div class="relative w-10/12 md:w-1/2 h-1/2 md:h-3/4 bg-white rounded shadow-lg">
   
    {{!-- button close --}}
    <button class="absolute -top-3 -right-3 bg-red-500 hover:bg-red-500 text-2xl w-10 h-10 rounded-full focus:outline-none text-white">
      &cross;
    </button>

    {{!-- header --}}
    <div class="px-4 py-3 border-b border-gray-200">
      <h2 class="text-xl font-semibold text-gray-600">SigUp</h2>
    </div>

    {{!-- body --}}
    <div class="w-full p-3">
      <div class="field">
        <label class="label">Username</label>
        <div class="control">
          <input class="input" type="text">
        </div>
      </div>
      <div class="field">
        <label class="label">Password</label>
        <div class="control">
          <input class="input" type="password">
        </div>
      </div>

      {{!-- footer --}}
      <div class="absolute bottom-0 left-0 px-4 py-3 border-t border-gray-200 w-full flex justify-end items-center">
        <button class="bg-gray-200 hover:bg-gray-300 text-gray-700 font-semibold py-2 px-4 rounded-l">
          Cancel
        </button>
        <button class="bg-blue-500 hover:bg-blue-700 text-white font-semibold py-2 px-4 rounded-r">
          Submit
        </button>

      </div>

    </div>

  </div>

</div>