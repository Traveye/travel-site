<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Travel Journal</title>
  {{!-- <link rel="icon" href="{{logo.imagePath}}" type="image/x-icon"> --}}
  <link href="https://cdnjs.cloudflare.com/ajax/libs/flowbite/1.6.3/flowbite.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://unpkg.com/leaflet@1.7.1/dist/leaflet.css"
  integrity="sha512-xodZBNTC5n17Xt2atTPuE1HxjVMSvLVW9ocqUKLsCC5CXdbqCmblAshOMAS6/keqq/sMZMZ19scR4PsZChSR7A=="
  crossorigin=""/>
  <link rel="stylesheet" href="https://unpkg.com/leaflet-control-geocoder/dist/Control.Geocoder.css"/>
  <link href="/output.css" rel="stylesheet">
  {{#unless showNav}}
  <link href="/css/login.css" rel="stylesheet">
  {{/unless}}
</head>
<body>
  <header>
  {{#if showNav}}
<nav id="header" class="w-full z-30 top-10 py-1 bg-white shadow-lg border-b mt-2">      
      <nav class="flex" style="width: 100%; justify-content: space-between; align-items: center">
         <img src="{{logo.imagePath}}" alt="{{logo.imageAlt}}" style="height: 2em; width: 8em; margin: .5em">
         <p>{{display_name}}'s Travel Journal</p>
        <ul class="flex" style="align-items: center;">
          <li>
           <a class="inline-block no-underline font-medium text-lg py-2 px-4 lg:-ml-2" href="/dashboard">Home</a>
          </li>
          <li>
            <button class="inline-block no-underline font-medium text-lg py-2 px-4 lg:-ml-2" id="logout">LogOut</button>
          </li>  
        </ul>
      </nav>
  </div>
</nav>
  {{/if}}
  </header>
    {{{ body }}}
  <footer>
  </footer>
  {{#if showNav}}
  <script src="/JS/logout.js"></script>
  {{/if}}

  
</body>

</html>
